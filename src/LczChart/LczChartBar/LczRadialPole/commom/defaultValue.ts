import { PoleConfig } from '../type'

const defaultPoleConfig: PoleConfig = {
  columnStyle: {
    outRadius: 60,
    inRadius: 20,
    bargap: 50,
    barCategoryGap: 40
  },
  columnBg: {
    display: true,
    color: '#B4B4B4',
    opacity: 20
  }
}

export { defaultPoleConfig }
